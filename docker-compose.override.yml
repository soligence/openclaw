# Fork-local overrides (soligence/openclaw).
# Docker Compose auto-merges this on top of docker-compose.yml.
#
# Named volumes replace upstream's bind-mount env vars.
# Create them once:
#   docker volume create openclaw-config
#   docker volume create openclaw-workspace

services:
  openclaw-gateway:
    user: "1000:1000" # node user — named volumes have correct ownership
    environment:
      CLAUDE_AI_SESSION_KEY: ""   # unset — not used in this fork
      CLAUDE_WEB_SESSION_KEY: ""  # unset — not used in this fork
      CLAUDE_WEB_COOKIE: ""       # unset — not used in this fork
    volumes:
      - openclaw-config:/home/node/.openclaw
      - openclaw-workspace:/home/node/.openclaw/workspace

  openclaw-cli:
    environment:
      CLAUDE_AI_SESSION_KEY: ""   # unset — not used in this fork
      CLAUDE_WEB_SESSION_KEY: ""  # unset — not used in this fork
      CLAUDE_WEB_COOKIE: ""       # unset — not used in this fork
    volumes:
      - openclaw-config:/home/node/.openclaw
      - openclaw-workspace:/home/node/.openclaw/workspace

volumes:
  openclaw-config:
    external: true
  openclaw-workspace:
    external: true
